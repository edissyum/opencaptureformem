name: Deployement OpenCaptureformem
on:
  push:
    branches: [ dev_cicd ]
  #pull_request:
    #branches: [ master ]

jobs:

  test-self-hosted:
    name: Test d'exécuteurs auto-hebergés
    runs-on: self-hosted
    defaults:
        run:
          working-directory: /

    steps: 
      - name: System info
        run: lsb_release -a
        